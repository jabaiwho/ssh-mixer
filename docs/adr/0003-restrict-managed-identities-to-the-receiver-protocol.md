# Restrict Managed Identities to the Receiver Protocol

Keys generated by SSH-mixer are dedicated per receiver and must be remotely restricted to a fixed, versioned Receiver Protocol with no shell, arbitrary command, forwarding, PTY, or startup-script access. Setup fails closed if those restrictions cannot be verified; user-owned identities remain an explicit, clearly labelled compatibility fallback.
