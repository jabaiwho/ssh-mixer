# Ship cross-platform Companion Setup

The first public release includes auditable Companion Setup scripts and Receiver Protocol adapters for Windows, Linux, and macOS because a source computer cannot securely configure a receiver that has no existing access channel. Artifacts are immutable, checksummed, and release-signed; macOS behavior is labelled Experimental until real-device verification is available, and every platform receives structured diagnostics.
