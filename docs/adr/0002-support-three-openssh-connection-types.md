# Support three OpenSSH connection types

SSH-mixer uses standard OpenSSH and supports Tailscale Connections, OpenSSH Profile Connections, and Direct SSH Connections. Tailscale is recommended and verified rather than required, while provider-specific behavior remains behind user-owned OpenSSH profiles so the streaming model does not depend on one network provider.
