# Stop sensitive capture at lock boundaries

Screen lock, suspend, logout, and disconnection are privacy boundaries rather than temporary transport interruptions. Sessions stop by default and never restart automatically; users may choose to continue non-microphone audio while locked, but Capture Sources always stop and require a later explicit start.
