# Require user-authorized automation

SSH-mixer will explain security-relevant setup, networking, privilege, update, migration, and removal decisions and require the user to approve or decline them. After approval, SSH-mixer performs and verifies the work itself; it does not hide changes, collect administrator passwords, or make users copy commands as the normal workflow.
